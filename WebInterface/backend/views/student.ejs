<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Student</h1>
  <button onclick="window.location.href='/'">Go Back</button>
  <form method="GET" action="/student" id="sortForm">
    <input type="text" name="search" placeholder="Search by Student ID" value="<%= search %>">
    <select name="sort" onchange="document.getElementById('sortForm').submit()">
      <option value="StudentID" <%= sort === 'StudentID' ? 'selected' : '' %>>Student ID</option>
      <!-- Add more sort options if needed -->
    </select>
    <button type="submit">Search</button>
  </form>
  <table>
    <thead>
      <tr>
        <th>StudentID</th>
        <!-- Add more columns if needed -->
      </tr>
    </thead>
    <tbody>
      <% student.forEach(stu => { %>
        <tr>
          <td><%= stu.StudentID %></td>
          <!-- Add more columns if needed -->
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>